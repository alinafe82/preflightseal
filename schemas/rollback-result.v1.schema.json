{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://preflightseal.dev/schemas/rollback-result.v1.schema.json",
  "title": "PreflightSeal Rollback Result v1",
  "type": "object",
  "required": [
    "schemaVersion",
    "receiptId",
    "transactionId",
    "planSeal",
    "rolledBackAt",
    "ok",
    "conflicts",
    "operations",
    "rollbackDigest"
  ],
  "properties": {
    "schemaVersion": { "const": "preflightseal.rollback-result.v1" },
    "receiptId": { "type": "string", "minLength": 1 },
    "transactionId": { "type": "string", "minLength": 1 },
    "planSeal": { "type": "string", "pattern": "^[a-fA-F0-9]{64}$" },
    "rolledBackAt": { "type": "string", "format": "date-time" },
    "ok": { "type": "boolean" },
    "conflicts": { "type": "array", "items": { "type": "string" } },
    "operations": { "type": "array", "items": { "type": "string" } },
    "rollbackDigest": { "type": "string", "pattern": "^[a-fA-F0-9]{64}$" }
  },
  "additionalProperties": false
}
